management:
  endpoint:
    shutdown:
      enabled: true

server:
  port: 8080

spring:
  application:
    name: gateway

logging:
  level:
    ROOT: info
    com.dgtedr: debug
  file: log/dgtedr-gateway.log

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka