import { NgModule } from '@angular/core';
import { NumberInput } from './directives';

export const routes = [
    {}
];

@NgModule({
    imports: [
    ],
    declarations: [
        NumberInput
    ],
    exports: [
        NumberInput
    ]
})

export class UtilsModule { }
